{
  "name": "root",
  "private": true,
  "volta": {
    "node": "16.13.2",
    "yarn": "1.22.18"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "packages/*/examples/*"
    ]
  },
  "devDependencies": {
    "husky": "^6.0.0"
  },
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx,.js --cache --cache-strategy content",
    "format": "eslint . --ext .ts,.tsx,.js --cache --cache-strategy content --fix"
  },
  "version": "0.0.0"
}
